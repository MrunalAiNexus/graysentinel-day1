# GraySentinel Project 02 Dependencies
#
# Core Engine Architecture:
# Built 100% on the Python Standard Library (Python 3.10+).
# No external pip dependencies are required for production CLI execution,
# finding modeling, evidence cryptographic hashing, validation, or test execution.
#
# Optional Developer / Linter Tooling (not strictly required for local execution):
# pytest>=7.0.0
# flake8>=6.0.0
